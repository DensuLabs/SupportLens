# ══════════════════════════════════════════════════════════════════════
# SupportLens v1.0 — requirements.txt
# 100% Free Stack: Groq + Ollama + PostgreSQL + pgvector
# ══════════════════════════════════════════════════════════════════════

# ── Core framework ────────────────────────────────────────────────────
streamlit>=1.35.0
phidata>=2.4.0

# ── LLM (Groq — free API, no credit card needed) ─────────────────────
groq>=0.9.0

# ── Embeddings (Ollama — free, runs locally) ──────────────────────────
# Install Ollama separately: curl -fsSL https://ollama.com/install.sh | sh
# Then run: ollama pull nomic-embed-text
ollama>=0.2.0

# ── Vector DB & Storage ───────────────────────────────────────────────
pgvector>=0.2.5
psycopg[binary]>=3.1.0
sqlalchemy>=2.0.0

# ── EVTX Parsers (install BOTH for fallback support) ──────────────────
python-evtx>=0.7.4
evtx>=0.8.2

# ── URL & HTTP fetching (for URL/GitHub ingestion) ────────────────────
requests>=2.31.0
beautifulsoup4>=4.12.0

# ── Log parsing utilities ─────────────────────────────────────────────
lxml>=5.0.0
